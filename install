#
# installer for https://github.com/jessetane/ctrls
#

# vars
REPO="https://github.com/jessetane/ctrls"
BRANCH="pdx"

# temp dir
TMP="tmp$(date +%s)"
mkdir "$TMP" && cd $_

# get repo/branch
git init
git fetch "$REPO" "$BRANCH":refs/remotes/origin/"$BRANCH"
git checkout "$BRANCH"

# cleanup
cd ..
mv "$TMP"/lib ctrls
rm -rf "$TMP"